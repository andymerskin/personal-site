---
export interface Props {
  heading?: string;
}

const { heading } = Astro.props;
---

<div class="rounded-lg border border-gray-200 bg-white p-6 shadow-sm">
  {heading && <h3 class="mb-4 text-lg font-bold text-gray-900">{heading}</h3>}
  <slot />
</div>
