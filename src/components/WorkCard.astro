---
import type { CollectionEntry } from "astro:content";

const { entry } = Astro.props as { entry: CollectionEntry<"work"> };
---

<li>
  <a
    href={`/work/${entry.id}/`}
    class="group block focus:outline-none"
    aria-label={entry.data.title}
  >
    <div
      class:list={[
        "aspect-4/3 w-full rounded-lg overflow-hidden",
        // "bg-shade-primary/2 dark:bg-white",
        // "ring-1 ring-shade-primary/5 dark:ring-neutral-700/60",
        "transition",
        "group-hover:ring-shade-primary/10 dark:group-hover:ring-neutral-600/70",
        "group-focus-visible:ring-2 group-focus-visible:ring-amber-500 group-focus-visible:ring-offset-2",
        "ring-offset-white dark:ring-offset-neutral-950",
        entry.data.classes,
      ]}
    >
      <div class="flex h-full w-full items-center justify-center p-6">
        <img
          src={entry.data.logo.src}
          alt=""
          class="max-h-[60%] max-w-[85%] object-contain"
          loading="lazy"
          decoding="async"
        />
      </div>
    </div>

    <div class="mt-3">
      <div class="text-lg font-bold leading-tight text-center">
        <span
          class="group-hover:underline group-hover:underline-offset-4 decoration-amber-500/80"
        >
          {entry.data.title}
        </span>
      </div>
      <div
        class="mt-1 text-sm text-center text-neutral-600 dark:text-neutral-400"
      >
        {entry.data.type}
      </div>
      <div class="text-xs text-center text-neutral-600 dark:text-neutral-400">
        {entry.data.year}
      </div>
    </div>
  </a>
</li>
